language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_install:
  - chmod +x gradlew

# Notify after CI is completed
notifications:
  email:
    recipients:
      - jungsj11@gmail.com