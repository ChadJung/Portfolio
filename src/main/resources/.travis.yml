language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# Notify after CI is completed
notifications:
  email:
    recipients:
      - jungsj11@gmail.com